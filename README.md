# dotfiles

```
mv rcrc to ~/.rcrc
rcup -f to symlink
```

```
git clone https://github.com/zsh-users/zsh-history-substring-search.git ~/.oh-my-zsh/plugins/history-substring-search
git clone https://github.com/iam4x/zsh-iterm-touchbar.git ~/.oh-my-zsh/plugins/zsh-iterm-touchbar
```

Fsql
```
# with Go installed
go get -v github.com/kshvmdn/fsql/...
```
